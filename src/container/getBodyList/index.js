import React, { Component } from 'react';
import store from '../../mobx/index'
import {getCategory} from '../../api/index'
import {observer} from 'mobx-react'

@observer
export default class getBodyList extends Component {
  
  componentDidMount() {
    
  }
  render() {
    return (
      <div>
      </div>
    )
  }
}
